name: Build custome
on: 
  workflow_dispatch:
  push:
    branches: 
      - main

env:
  NODE_OPTIONS: "--max-old-space-size=4096"
  JAVA_TOOL_OPTIONS: "-Xmx4g"
  MB_EDITION: "oss"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Check Repository Structure
      run: |
        echo "📂 Kiểm tra cấu trúc repository..."
        ls -la
        
        echo "🔍 Tìm file build scripts..."
        find . -name "pom.xml" -o -name "deps.edn" -o -name "build.clj" -o -name "build.sh" | head -20
        
        echo "📦 Kiểm tra thư mục frontend..."
        ls -la frontend/ 2>/dev/null || echo "⚠️ Không tìm thấy thư mục frontend"
        
        echo "☕ Kiểm tra file Clojure build..."
        ls -la bin/ 2>/dev/null || echo "⚠️ Không tìm thấy thư mục bin"
        
    - name: Set up Node.js 22
      uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: 'yarn'
        
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'
        
    - name: Install Clojure
      uses: DeLaGuardo/setup-clojure@12.1
      with:
        cli: 'latest'
        
    - name: Cache Clojure Dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.m2/repository
          ~/.gitlibs
          ~/.clojure
        key: clojure-${{ hashFiles('deps.edn', 'build.clj') }}
        restore-keys: clojure-
        
    - name: Install Frontend Dependencies
      run: |
        echo "📦 Cài đặt thư viện frontend..."
        
        if [ -d "frontend" ]; then
          cd frontend
          yarn install --frozen-lockfile
        else
          echo "⚠️ Không tìm thấy thư mục frontend, cài đặt ở root..."
          yarn install --frozen-lockfile
        fi
        
    - name: Fix Crypto Issue (Package.json Patch)
      run: |
        echo "🔧 Kiểm tra và fix lỗi crypto..."
        
        # Di chuyển vào thư mục có package.json
        if [ -d "frontend" ]; then
          cd frontend
        fi
        
        # Backup package.json gốc
        cp package.json package.json.backup
        
        # Kiểm tra xem đã có cấu hình browser chưa
        if grep -q '"browser"' package.json; then
          echo "⚠️  package.json đã có cấu hình browser, bỏ qua patch"
        else
          echo "✅ Thêm cấu hình browser để fix crypto"
          sed -i '/"dependencies":/i\  "browser": {\n    "crypto": false\n  },' package.json
        fi
        
        # Hiển thị diff để kiểm tra
        echo "📋 Thay đổi trong package.json:"
        diff package.json.backup package.json || true
        
        # Kiểm tra syntax JSON có hợp lệ không
        echo "🔍 Kiểm tra JSON syntax..."
        node -e "JSON.parse(require('fs').readFileSync('package.json', 'utf8'))" && echo "✅ JSON hợp lệ" || echo "❌ JSON không hợp lệ"
        
    - name: Build Frontend
      run: |
        echo "🎨 Build giao diện frontend..."
        
        if [ -d "frontend" ]; then
          cd frontend
        fi
        
        yarn build
        
        echo "✅ Frontend build hoàn tất"
        
    - name: Make Build Script Executable
      run: |
        if [ -f "bin/build.sh" ]; then
          chmod +x bin/build.sh
        fi
        if [ -f "build.sh" ]; then
          chmod +x build.sh
        fi
      
    - name: Build Backend (Uberjar)
      run: |
        echo "☕ Build backend Metabase..."
        
        if [ -f "bin/build.sh" ]; then
          echo "✅ Sử dụng bin/build.sh"
          ./bin/build.sh
        elif [ -f "build.sh" ]; then
          echo "✅ Sử dụng build.sh ở root"
          ./build.sh
        elif [ -f "build.clj" ]; then
          echo "✅ Build bằng Clojure build.clj"
          clojure -T:build uberjar
        elif [ -f "deps.edn" ]; then
          echo "✅ Build bằng deps.edn"
          clojure -X:build uberjar
        else
          echo "❌ Không tìm thấy script build!"
          echo "🔍 Các file có sẵn:"
          ls -la
          exit 1
        fi
        
        echo "✅ Backend build hoàn tất"
        
        echo "🔍 Tìm file JAR đã build..."
        find . -name "*.jar" -type f
        
    - name: Verify JAR File
      run: |
        echo "📊 Thông tin file JAR:"
        
        # Tìm file JAR trong các thư mục có thể có
        JAR_FILE=$(find . -name "metabase*.jar" -o -name "*.jar" | grep -E "target|build|out" | head -1)
        
        if [ -z "$JAR_FILE" ]; then
          echo "❌ Không tìm thấy file JAR!"
          echo "🔍 Tìm kiếm trong toàn bộ project:"
          find . -name "*.jar" -type f
          exit 1
        fi
        
        echo "✅ Tìm thấy JAR: $JAR_FILE"
        ls -lh "$JAR_FILE"
        
        echo "📦 Kích thước file:"
        du -h "$JAR_FILE"
        
        echo "🔍 Kiểm tra JAR có hợp lệ:"
        jar tf "$JAR_FILE" | head -20
        
        # Lưu đường dẫn JAR để dùng cho step sau
        echo "JAR_PATH=$JAR_FILE" >> $GITHUB_ENV
        
    - name: Upload Metabase JAR
      uses: actions/upload-artifact@v4
      with:
        name: metabase-vietnamese-v${{ github.run_number }}
        path: ${{ env.JAR_PATH }}
        retention-days: 7
        compression-level: 0
        
    - name: Create Release (If Tagged)
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v1
      with:
        files: ${{ env.JAR_PATH }}
        name: Metabase Vietnamese ${{ github.ref_name }}
        body: |
          🎉 Metabase Vietnamese Edition
          
          ## Cách sử dụng
          ```bash
          java -jar metabase.jar
          ```
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Notify Build Success
      if: success()
      run: |
        echo "✅ BUILD THÀNH CÔNG!"
        echo "📥 Tải file JAR tại: Actions → Artifacts"
        echo "🚀 File: metabase-vietnamese-v${{ github.run_number }}"
        
    - name: Notify Build Failed
      if: failure()
      run: |
        echo "❌ BUILD THẤT BẠI!"
        echo "🔍 Kiểm tra logs để tìm lỗi"
